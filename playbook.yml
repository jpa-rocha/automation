---
- name: Setup workstation
  hosts: workstation
  connection: local
  become: true

  pre_tasks:
    - name: Update package cache
      tags: always
      ansible.builtin.dnf:
        update_cache: true
      changed_when: false
      when: ansible_distribution == "Fedora"

  roles:
    - workstation
