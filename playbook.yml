---
- name: Setup Workstation
  hosts: workstation
  connection: local
  become: true

  tasks:
    - name: Ping my hosts
      ansible.builtin.ping:

    - name: Print message
      ansible.builtin.debug:
        msg: Hello World
